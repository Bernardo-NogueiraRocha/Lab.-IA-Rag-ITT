name: blip-env
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pytorch=2.5.1
  - pytorch-cuda=12.1  # Ajuste de acordo com a sua GPU/CUDA
  - pillow
  - pip:
      - transformers==4.41.0
      - accelerate
      - safetensors
